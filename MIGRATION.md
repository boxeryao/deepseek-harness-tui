# Standalone migration status

The repository contains only TUI-owned source, tests, bundle configuration, documentation, license, and images. All Harness dependencies resolve from npm at the `0.1.0-rc.6` release line. A clean install, TypeScript build, and the focused test suite pass outside the DeepSeek Harness monorepo.

The remaining publication step is deliberate: initialize and review the standalone Git history before replacing the existing GitHub repository contents.

