/**
 * TUI bundles need no command-line startup provider: the terminal driver owns
 * the interactive input loop after the configured tree settles.
 * @module deepseek-harness-tui/startup
 */

export {}
